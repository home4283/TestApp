<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a10510323.user.test2">

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <!--<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".place" />
        <activity android:name=".hotel" />
        <activity android:name=".bus" />
        <activity android:name=".park" />
        <activity android:name=".fg_place" />
        <activity android:name=".fg_hotel" />
        <activity android:name=".fg_bus" />
        <activity android:name=".fg_park" />
        <activity android:name=".Place2" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDeS4r7ssPxGhX-btgvQMWwiS3dFNecA7Q" />

        <activity android:name=".hotel2"/>
        <activity android:name=".park2"/>
    </application>

</manifest>